services:
  hooker:
    build:
      context: .
      args:
        HOOKER_VERSION: "0.0.0-dev"
    ports:
      - "127.0.0.1:10804:10804"
    environment:
      DB_PATH: /data/hooker.db
    volumes:
      - hooker-data:/data
    restart: unless-stopped

volumes:
  hooker-data:
