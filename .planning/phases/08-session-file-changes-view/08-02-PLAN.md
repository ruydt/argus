---
phase: 08-session-file-changes-view
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - frontend/src/features/sessions/FileChangesDrawer.tsx
  - frontend/src/features/sessions/FileChangesList.tsx
autonomous: true
requirements:
  - SESS-01
  - SESS-02

must_haves:
  truths:
    - "D-07: Changed files are paginated by file group, not by individual change event"
    - "Default file page size is 25 unless implementation documents a better compact value"
    - "D-08: Each file row toggles expanded/collapsed state, exposes `aria-expanded`, and shows that file's change events without internal per-file pagination"
    - "D-09: Expanded file rows show old/new snippets per change, not a full GitHub-style file diff"
    - "D-10: Each expanded change entry shows timestamp, tool/action, available line number, and compact old/new snippets"
    - "D-11: No blank old/new code block is rendered for absent snippet strings; metadata-only changes show the required fallback note"
    - "Snippet text is rendered as text, preserves whitespace, wraps long lines, and does not use raw HTML injection"
  artifacts:
    - path: frontend/src/features/sessions/FileChangesList.tsx
      provides: "Paginated route-level file-change list and snippet rendering"
      contains: "Before"
    - path: frontend/src/features/sessions/FileChangesDrawer.tsx
      provides: "Reusable file-change helpers or compatibility wrapper after refactor"
      contains: "FileChangesList"
  key_links:
    - from: frontend/src/features/sessions/TraceViewPage.tsx
      to: frontend/src/features/sessions/FileChangesList.tsx
      via: "route page renders paginated file list"
      pattern: "FileChangesList"
    - from: frontend/src/features/sessions/FileChangesList.tsx
      to: frontend/src/types/sessions.ts
      via: "FileChangeGroup and FileChangeEvent props"
      pattern: "FileChangeGroup"
---

<objective>
Build the paginated file list and per-change snippet rendering for the new session file-change browser.

Purpose: Satisfy SESS-01 and SESS-02 by making file groups paginated and expandable, with every change showing timestamp, tool/action, line metadata, and compact old/new line snippets.
Output: Route-level file-change components with accessible disclosure controls, shadcn pagination controls, and old/new snippet blocks.
</objective>

<execution_context>
@/Users/duytran/GitHub/hooker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/duytran/GitHub/hooker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/duytran/GitHub/hooker/.planning/ROADMAP.md
@/Users/duytran/GitHub/hooker/.planning/REQUIREMENTS.md
@/Users/duytran/GitHub/hooker/.planning/phases/08-session-file-changes-view/08-CONTEXT.md
@/Users/duytran/GitHub/hooker/.planning/phases/08-session-file-changes-view/08-UI-SPEC.md
@/Users/duytran/GitHub/hooker/.planning/phases/08-session-file-changes-view/08-RESEARCH.md
@/Users/duytran/GitHub/hooker/.planning/phases/08-session-file-changes-view/08-PATTERNS.md

<interfaces>
Existing drawer helpers worth reusing or extracting:
- `toolColor(tool?: string)`
- `toolLabel(tool?: string, action?: string)`
- `FileIcon`
- `shortenPath(path: string)`
- `formatRelativeTime(time: string, sessionStartedAt?: string)`

Existing UI primitives:
- `frontend/src/components/ui/button.tsx`
- `frontend/src/components/ui/badge.tsx`
- `frontend/src/components/ui/separator.tsx`
- `frontend/src/components/ui/skeleton.tsx`
- `frontend/src/components/ui/empty.tsx`
- `frontend/src/components/ui/card.tsx`

Snippet fields:
- `old_string` renders under `Before`
- `new_string` renders under `After`
- `start_line` renders as `L{start_line}` when present
</interfaces>
</context>

<threat_model>
File paths and snippets are local captured content and may contain arbitrary text. Render them as escaped React text only. Do not use `dangerouslySetInnerHTML`. Avoid building CSS class names directly from captured tool or path values. Bound snippet panels with max height/scroll so one large captured value cannot break the page layout.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Extract route-level file-change list from drawer logic</name>
  <files>frontend/src/features/sessions/FileChangesList.tsx, frontend/src/features/sessions/FileChangesDrawer.tsx</files>
  <read_first>
    - frontend/src/features/sessions/FileChangesDrawer.tsx (full file)
    - frontend/src/types/sessions.ts (FileChangeGroup and FileChangeEvent)
    - frontend/src/components/ui/button.tsx
    - frontend/src/components/ui/badge.tsx
    - frontend/src/components/ui/card.tsx
  </read_first>
  <action>
Create a route-level `FileChangesList` component or equivalent feature-local component. Reuse useful drawer helpers by extracting them to the new file or keeping them exported from `FileChangesDrawer.tsx` if that is less invasive.

The component must accept `groups`, `sessionStartedAt`, and pagination state/handlers or own local pagination state. It must render collapsed file rows with file icon, shortened path, change count badge, latest/relative timestamp, and a disclosure indicator.

Keep `FileChangesDrawer` only if still used elsewhere; if retained, make it consume the same list helpers so old logic is not duplicated.
  </action>
  <verify>
    <automated>cd /Users/duytran/GitHub/hooker/frontend && pnpm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Route-level file list exists outside the side-drawer-only layout
    - File row disclosure uses a keyboard reachable `Button` or button-like primitive
    - File row exposes `aria-expanded`
    - File paths are mono and truncate/wrap according to the UI spec
    - Existing drawer-specific close button and fixed side-panel shell are not part of the route-level list
  </acceptance_criteria>
  <done>File-change list is reusable as a normal page section</done>
</task>

<task type="auto">
  <name>Task 2: Add file pagination controls</name>
  <files>frontend/src/features/sessions/FileChangesList.tsx, frontend/src/components/shared/PaginationBar.tsx</files>
  <read_first>
    - frontend/src/components/shared/PaginationBar.tsx (full file)
    - .planning/phases/08-session-file-changes-view/08-UI-SPEC.md (File Pagination section)
  </read_first>
  <action>
Implement file-level pagination over `FileChangeGroup[]`. Use a default page size of 25. Show current range and total files, for example `1-25 of 83 files`. Provide first, previous, next, and last controls when there is more than one page. Disable controls at bounds and reset expanded rows when changing pages if that keeps state simpler.

If reusing `PaginationBar`, update it to use shadcn `Button` controls rather than raw buttons. If that shared refactor risks other features, implement a local pagination control inside the sessions feature and leave the shared component untouched.
  </action>
  <verify>
    <automated>cd /Users/duytran/GitHub/hooker/frontend && pnpm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Pagination slices file groups, not change events
    - Range copy is visible when more than zero files exist
    - First/previous are disabled on page 1
    - Next/last are disabled on the last page
    - Changing pages cannot show an expanded row from a different page under stale state
  </acceptance_criteria>
  <done>Changed files are paginated with accessible shadcn controls</done>
</task>

<task type="auto">
  <name>Task 3: Render per-change old/new snippets</name>
  <files>frontend/src/features/sessions/FileChangesList.tsx</files>
  <read_first>
    - frontend/src/types/sessions.ts (FileChangeEvent fields)
    - .planning/phases/08-session-file-changes-view/08-UI-SPEC.md (Snippet Contract section)
  </read_first>
  <action>
Inside each expanded file row, render a vertical list of change entries. Each entry must include relative timestamp from session start when available, tool/action label, `L{start_line}` badge when present, `Before` block when `old_string` exists, and `After` block when `new_string` exists.

Do not render empty code blocks for missing values. When both strings are absent, render the exact metadata-only note: `No inline snippet captured for this change.` Use `whitespace-pre-wrap`, long-line wrapping, and a max-height guard for large snippets.
  </action>
  <verify>
    <automated>cd /Users/duytran/GitHub/hooker/frontend && pnpm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Expanded rows show per-change timestamp
    - Expanded rows show tool/action label
    - Expanded rows show `L{start_line}` when `start_line` is present
    - Old snippets use the visible label `Before`
    - New snippets use the visible label `After`
    - Metadata-only changes show `No inline snippet captured for this change.`
  </acceptance_criteria>
  <done>Expanded file rows show compact old/new snippets for each change</done>
</task>

</tasks>

<verification>
- `cd /Users/duytran/GitHub/hooker/frontend && pnpm run typecheck`
- `cd /Users/duytran/GitHub/hooker/frontend && pnpm run lint`
- `cd /Users/duytran/GitHub/hooker/frontend && pnpm run test -- tests/features/sessions`
- Manual browser smoke on desktop and mobile widths for header wrapping, pagination, expanded snippets, and focus states.
</verification>

<success_criteria>
- SESS-01: File groups are displayed with file pagination.
- SESS-02: Every expanded file row can show change timestamp, tool/action, line metadata, and old/new snippets.
- UI-SPEC: Pagination and disclosure controls use appropriate primitives and remain compact.
</success_criteria>
