---
phase: 08-session-file-changes-view
plan: 05
type: gap_closure
wave: gap
depends_on:
  - 08-04
files_modified:
  - backend/internal/agents/codex/codex.go
  - backend/internal/repository/sqlite/sqlite.go
  - backend/tests/internal/repository/sqlite/file_changes_legacy_test.go
autonomous: true
requirements:
  - SESS-02
  - SESS-03
gap_source: 08-UAT.md

must_haves:
  truths:
    - "Historical Codex apply_patch rows stored with command text but empty old_string/new_string are visible as session file changes"
    - "Codex apply_patch file-change counts are non-zero for sessions with patch edits"
    - "The read path does not mutate backend/hooker.db or expose raw command payloads to the frontend"
  artifacts:
    - path: backend/tests/internal/repository/sqlite/file_changes_legacy_test.go
      provides: "Repository regression for legacy command-only Codex apply_patch rows"
      contains: "TestGetFileChangesBackfillsLegacyCodexApplyPatchCommand"
    - path: backend/internal/repository/sqlite/sqlite.go
      provides: "Read-path fallback that derives old/new snippets from stored apply_patch command text"
      contains: "apply_patch"
---

<objective>
Close the remaining Phase 8 UAT gap where existing Codex sessions in `backend/hooker.db` still show no changed files because historical `apply_patch` rows have path and command data but empty old/new snippet columns.
</objective>

<context>
08-04 fixed future normalization, but DB inspection found existing Codex rows still had:

- `tool_name = apply_patch`
- non-empty `path`
- patch command text in `command`
- empty `old_string` and `new_string`

Because the file-change condition required known edit tools or old/new strings, those rows were omitted from `/api/file-changes` and from session file-change counts.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add legacy Codex apply_patch repository regression</name>
  <files>
    backend/tests/internal/repository/sqlite/file_changes_legacy_test.go
  </files>
  <action>
Add a repository test that inserts a legacy Codex `apply_patch` event with path and command but no old/new strings, then verifies:

- `GetFileChanges` returns one changed file group
- old/new snippets are derived from the patch command
- `GetSessionFileChangeCounts` counts the changed file
  </action>
  <verify>
    <automated>cd backend && GOCACHE=/private/tmp/hooker-gocache go test ./tests/internal/repository/sqlite -run TestGetFileChangesBackfillsLegacyCodexApplyPatchCommand -count=1</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Backfill legacy rows on the file-change read path</name>
  <files>
    backend/internal/agents/codex/codex.go
    backend/internal/repository/sqlite/sqlite.go
  </files>
  <action>
Expose the Codex patch snippet helper and update the SQLite file-change query to include `apply_patch` rows. When a returned row has empty old/new strings but contains a patch command, parse the command and derive old/new snippets for the API response.
  </action>
  <verify>
    <automated>cd backend && GOCACHE=/private/tmp/hooker-gocache go test ./tests/internal/agents/codex ./tests/internal/handler ./tests/internal/repository/sqlite</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Re-run focused Phase 8 frontend regression</name>
  <files>
    frontend/tests/features/sessions/project-session-traces.test.tsx
    frontend/tests/features/sessions/useFileChanges.test.ts
  </files>
  <action>
Run the existing session file-change frontend tests to confirm the API contract remains compatible with the UI.
  </action>
  <verify>
    <automated>pnpm --dir frontend test --run tests/features/sessions/project-session-traces.test.tsx tests/features/sessions/useFileChanges.test.ts</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- Existing Codex sessions with legacy apply_patch rows can show changed files without DB mutation.
- Expanded file rows receive old/new snippets through the existing FileChangesList contract.
- Phase 8 UAT Test 3 can be marked passed.
</success_criteria>
