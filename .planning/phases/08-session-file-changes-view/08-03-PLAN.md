---
phase: 08-session-file-changes-view
plan: 03
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
files_modified:
  - frontend/tests/features/sessions/project-session-traces.test.tsx
  - frontend/tests/features/sessions/useFileChanges.test.ts
  - backend/tests/internal/handler/file_changes_contract_test.go
autonomous: true
requirements:
  - SESS-01
  - SESS-02
  - SESS-03

must_haves:
  truths:
    - "Frontend route tests cover loading, error, empty, populated, expanded snippet, and pagination behavior"
    - "Tests assert the route does not render old trace/timeline primary UI affordances"
    - "`useFileChanges` has focused coverage for fetch URL, success, ok:false error, and rejected fetch"
    - "Backend contract coverage proves `/api/file-changes` returns old/new/start_line fields when stored events include them, unless equivalent coverage already exists"
    - "Verification commands are run and any unrelated dirty-worktree failures are documented without reverting user changes"
  artifacts:
    - path: frontend/tests/features/sessions/project-session-traces.test.tsx
      provides: "Route-level session file-change page tests or updated replacement tests"
      contains: "File changes"
    - path: frontend/tests/features/sessions/useFileChanges.test.ts
      provides: "Hook tests for `/api/file-changes`"
      contains: "/api/file-changes?session_id="
    - path: backend/tests/internal/handler/file_changes_contract_test.go
      provides: "Handler contract test for old/new/start_line file-change response"
      contains: "FileChanges"
  key_links:
    - from: frontend/tests/features/sessions/project-session-traces.test.tsx
      to: frontend/src/features/sessions/TraceViewPage.tsx
      via: "MemoryRouter route render"
      pattern: "TraceViewPage"
    - from: frontend/tests/features/sessions/useFileChanges.test.ts
      to: frontend/src/features/sessions/hooks/useFileChanges.ts
      via: "renderHook"
      pattern: "useFileChanges"
---

<objective>
Add focused verification for the new session file-change page and the existing file-change data contract.

Purpose: Close SESS-01, SESS-02, and SESS-03 with tests that lock the route replacement, pagination, snippet rendering, and API-first assumption.
Output: Frontend route/hook tests plus backend contract coverage only where existing tests do not already prove old/new/start_line behavior.
</objective>

<execution_context>
@/Users/duytran/GitHub/hooker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/duytran/GitHub/hooker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/duytran/GitHub/hooker/.planning/ROADMAP.md
@/Users/duytran/GitHub/hooker/.planning/REQUIREMENTS.md
@/Users/duytran/GitHub/hooker/.planning/phases/08-session-file-changes-view/08-CONTEXT.md
@/Users/duytran/GitHub/hooker/.planning/phases/08-session-file-changes-view/08-UI-SPEC.md
@/Users/duytran/GitHub/hooker/.planning/phases/08-session-file-changes-view/08-RESEARCH.md
@/Users/duytran/GitHub/hooker/.planning/phases/08-session-file-changes-view/08-PATTERNS.md

<interfaces>
Frontend test pattern:
- `MemoryRouter`
- `Routes`
- `Route path="/sessions/:encodedCwd/:sessionId"`
- `vi.stubGlobal('fetch')`
- Testing Library `screen`, `waitFor`, and click/user-event interactions

Backend contract:
- `handler.FileChanges(svc)` returns HTTP 400 without `session_id`
- With matching stored events, the JSON response should include grouped file paths and change records with old/new/start_line values.
</interfaces>
</context>

<threat_model>
Tests must avoid network access and use stubbed `fetch`. Route tests should not depend on wall-clock relative copy when exact locale output can vary; prefer stable labels and exact snippet/tool/path values. Backend tests must use isolated in-memory SQLite fixtures and must not touch the developer's real DB.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Update route-level session tests for file-change page</name>
  <files>frontend/tests/features/sessions/project-session-traces.test.tsx</files>
  <read_first>
    - frontend/tests/features/sessions/project-session-traces.test.tsx (full file)
    - frontend/src/features/sessions/TraceViewPage.tsx (final implementation from Plans 01 and 02)
    - .planning/phases/08-session-file-changes-view/08-UI-SPEC.md (Verification Contract section)
  </read_first>
  <action>
Replace or add route-level tests that render `TraceViewPage` through `MemoryRouter` with encoded cwd and session ID params. Stub `/api/sessions?...` and `/api/file-changes?session_id=...` responses.

Cover loading, fetch error, empty file changes, populated file list, file pagination, and expanded row snippet rendering. Assert visible copy from the UI-SPEC including `File changes`, `{count} files changed`, `Loading file changes...`, `No file changes recorded for this session.`, `Before`, and `After`.

Add a negative assertion that trace/timeline affordances from the old page are not present in the route-level page, using stable text or roles from the old UI rather than component internals.
  </action>
  <verify>
    <automated>cd /Users/duytran/GitHub/hooker/frontend && pnpm run test -- tests/features/sessions/project-session-traces.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Test file includes route coverage for the new file-change page
    - Tests prove expanded rows render old/new snippets
    - Tests prove pagination changes visible file rows
    - Tests prove empty and error copy match the UI-SPEC
    - Tests do not require real backend network access
  </acceptance_criteria>
  <done>Route tests lock the new file-change browser behavior</done>
</task>

<task type="auto">
  <name>Task 2: Add useFileChanges hook tests</name>
  <files>frontend/tests/features/sessions/useFileChanges.test.ts</files>
  <read_first>
    - frontend/src/features/sessions/hooks/useFileChanges.ts (full file)
    - frontend/tests/features/sessions/useTraces.test.ts (hook test pattern)
  </read_first>
  <action>
Create `useFileChanges.test.ts` following the existing `useTraces` test style. Cover the fetch URL with encoded session ID, successful group parsing, ok:false error state, rejected fetch error state, and empty session ID behavior if the hook supports that branch.
  </action>
  <verify>
    <automated>cd /Users/duytran/GitHub/hooker/frontend && pnpm run test -- tests/features/sessions/useFileChanges.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Hook test asserts `/api/file-changes?session_id=${encodeURIComponent(sessionId)}`
    - Hook test asserts successful `groups` data includes `old_string`, `new_string`, and `start_line`
    - Hook test asserts error state for ok:false
    - Hook test asserts error state for rejected fetch
  </acceptance_criteria>
  <done>Hook tests cover the existing API-first file-change data path</done>
</task>

<task type="auto">
  <name>Task 3: Prove backend file-change contract if not already covered</name>
  <files>backend/tests/internal/handler/dashboard_health_usage_version_test.go, backend/tests/internal/repository/sqlite/sqlite_test.go</files>
  <read_first>
    - backend/internal/handler/file_changes.go (full file)
    - backend/internal/repository/sqlite/sqlite.go (GetFileChanges implementation)
    - backend/tests/internal/handler/dashboard_health_usage_version_test.go (file-change handler test)
    - backend/tests/internal/repository/sqlite/sqlite_test.go (existing repository tests)
  </read_first>
  <action>
Inspect existing backend tests for coverage that proves `GetFileChanges` and/or `handler.FileChanges` preserves `old_string`, `new_string`, and `start_line`. If equivalent coverage already exists, document it in the execution summary and do not add redundant backend tests.

If coverage is missing, add a narrow test using the existing test helpers and in-memory SQLite fixture. Insert or add an event for a session with path, old string, new string, and start line, call the file-change API/repository, and assert the response/group preserves those values.
  </action>
  <verify>
    <automated>cd /Users/duytran/GitHub/hooker/backend && go test ./tests/internal/handler ./tests/internal/repository/sqlite</automated>
  </verify>
  <acceptance_criteria>
    - Existing or new backend tests prove file-change old/new/start_line contract
    - No backend production files are changed unless the contract is found insufficient
    - Backend tests use isolated fixtures and do not touch the real DB
  </acceptance_criteria>
  <done>SESS-03 is backed by contract test coverage or documented existing coverage</done>
</task>

</tasks>

<verification>
- `cd /Users/duytran/GitHub/hooker/frontend && pnpm run typecheck`
- `cd /Users/duytran/GitHub/hooker/frontend && pnpm run lint`
- `cd /Users/duytran/GitHub/hooker/frontend && pnpm run test -- tests/features/sessions`
- `cd /Users/duytran/GitHub/hooker/backend && go test ./tests/internal/handler ./tests/internal/repository/sqlite` if backend tests are added or touched
- Manual UI smoke at desktop and mobile widths for header, pagination, row expansion, snippet wrapping, empty state, and focus visibility
</verification>

<success_criteria>
- SESS-01: Tests prove the route is now a paginated file-change browser.
- SESS-02: Tests prove expanded rows show per-change timestamp/tool/action/line/old/new details.
- SESS-03: Tests prove existing file-change API/data satisfies the page contract before backend changes are considered.
</success_criteria>
