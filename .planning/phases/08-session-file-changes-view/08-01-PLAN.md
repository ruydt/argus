---
phase: 08-session-file-changes-view
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/sessions/TraceViewPage.tsx
  - frontend/src/features/sessions/hooks/useFileChanges.ts
  - frontend/src/types/sessions.ts
autonomous: true
requirements:
  - SESS-01
  - SESS-03

must_haves:
  truths:
    - "D-01: `/sessions/:cwd/:sessionId` renders a file-change browser as the primary page, with no trace/timeline tab or secondary trace page retained in Phase 8"
    - "D-02: `TraceViewPage` no longer imports or renders `TraceTreeNode`, `EventTimeline`, `TraceInspectionPanel`, `useTraces`, zoom controls, split panels, or mobile trace overlay behavior"
    - "D-03: The page still resolves session metadata from the encoded cwd route param and renders compact breadcrumbs, session ID, started time, duration, and file-change count"
    - "D-04: The page uses `useFileChanges(sessionId)` as the primary file-change source and relies on existing `FileChangeGroup`/`FileChangeEvent` fields"
    - "D-05: No backend/API change is made unless the current file-change contract is proven insufficient"
    - "D-06: Any backend work, if implementation proves it is needed, must stay limited to file-change page support and must not add search/filter scope"
  artifacts:
    - path: frontend/src/features/sessions/TraceViewPage.tsx
      provides: "Route-level session file-change page shell"
      contains: "File changes"
    - path: frontend/src/features/sessions/hooks/useFileChanges.ts
      provides: "Existing `/api/file-changes?session_id=...` fetch hook"
      contains: "/api/file-changes?session_id="
    - path: frontend/src/types/sessions.ts
      provides: "FileChangeGroup and FileChangeEvent contract with old/new fields"
      contains: "old_string"
  key_links:
    - from: frontend/src/features/sessions/TraceViewPage.tsx
      to: frontend/src/features/sessions/hooks/useFileChanges.ts
      via: "route page calls hook"
      pattern: "useFileChanges\\(sessionId\\)"
    - from: frontend/src/features/sessions/TraceViewPage.tsx
      to: frontend/src/types/sessions.ts
      via: "FileChangeGroup rendering"
      pattern: "FileChangeGroup"
---

<objective>
Replace the current session trace route shell with a compact file-change page shell that keeps session metadata and switches the primary data source to file changes.

Purpose: Satisfy SESS-01 and SESS-03 by removing the trace/timeline experience from `/sessions/:cwd/:sessionId` and proving the existing file-change API is the first data path.
Output: A route-level page that shows the compact header, loading/error/empty/populated file-change states, and no trace/timeline controls.
</objective>

<execution_context>
@/Users/duytran/GitHub/hooker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/duytran/GitHub/hooker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/duytran/GitHub/hooker/.planning/ROADMAP.md
@/Users/duytran/GitHub/hooker/.planning/REQUIREMENTS.md
@/Users/duytran/GitHub/hooker/.planning/phases/08-session-file-changes-view/08-CONTEXT.md
@/Users/duytran/GitHub/hooker/.planning/phases/08-session-file-changes-view/08-UI-SPEC.md
@/Users/duytran/GitHub/hooker/.planning/phases/08-session-file-changes-view/08-RESEARCH.md
@/Users/duytran/GitHub/hooker/.planning/phases/08-session-file-changes-view/08-PATTERNS.md

<interfaces>
Current frontend data hook:
`useFileChanges(sessionId)` returns `{ groups, loading, error }` from `/api/file-changes?session_id=...`.

Current frontend types:
`FileChangeEvent` includes `time`, `tool`, `action`, `old_string`, `new_string`, and `start_line`.
`FileChangeGroup` includes `path`, `count`, and `changes`.

Current route:
`TraceViewPage` owns session metadata lookup, route params, current trace rendering, and the file-change drawer integration. The implementation should keep the route params and session metadata lookup but remove trace-specific state, imports, event selection, zoom state, split-panel layout, inspection panel refs, and drawer mode switching.
</interfaces>
</context>

<threat_model>
No new data source or authentication boundary is introduced. Main risks are accidental retention of trace UI, malformed route param handling, and rendering unbounded user-captured snippets. Mitigate by keeping existing API URLs encoded, rendering snippet content as React text rather than HTML, and limiting large snippet display in Plan 02.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Replace trace route state with session/file-change state</name>
  <files>frontend/src/features/sessions/TraceViewPage.tsx</files>
  <read_first>
    - frontend/src/features/sessions/TraceViewPage.tsx (full file)
    - frontend/src/features/sessions/hooks/useFileChanges.ts (full file)
    - frontend/src/features/sessions/utils.ts (full file)
    - frontend/src/types/sessions.ts (FileChangeGroup, FileChangeEvent, Session)
  </read_first>
  <action>
Update `TraceViewPage` so the route keeps only the state needed for session metadata and file-change rendering. Remove imports and state for `EventTimeline`, `TraceTreeNode`, `TraceInspectionPanel`, `useTraces`, selected span, panel mode, zoom controls, resizable panels, and mobile trace overlay.

Keep the existing encoded cwd/session route behavior. Continue to fetch sessions for the decoded cwd and derive the current `Session` by `session_id`. Add the `useFileChanges(sessionId)` call and pass the resulting data into the file-change list components introduced in Plan 02.

Render the copy required by the UI spec: `File changes`, `{count} files changed`, `Loading file changes...`, `No file changes recorded for this session.`, and `Failed to load file changes: {error}`.
  </action>
  <verify>
    <automated>cd /Users/duytran/GitHub/hooker/frontend && pnpm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `TraceViewPage.tsx` imports `useFileChanges`
    - `TraceViewPage.tsx` does not import `EventTimeline`, `TraceTreeNode`, `TraceInspectionPanel`, or `useTraces`
    - The route still links back to `/projects` and `/sessions/:encodedCwd`
    - The route renders the UI-SPEC page label `File changes`
    - Loading, error, empty, and populated file-change branches are represented
  </acceptance_criteria>
  <done>Route page is file-change focused and no longer owns trace/timeline state</done>
</task>

<task type="auto">
  <name>Task 2: Build compact header from session metadata</name>
  <files>frontend/src/features/sessions/TraceViewPage.tsx</files>
  <read_first>
    - frontend/src/features/sessions/utils.ts (sessionDurationMs and formatDuration)
    - frontend/src/types/sessions.ts (Session fields)
    - .planning/phases/08-session-file-changes-view/08-UI-SPEC.md (Compact Header section)
  </read_first>
  <action>
Implement the compact header described by the UI spec. It must show breadcrumbs, the project/cwd context, the shortened session ID, started time, duration, ended time when present, and file-change count.

Use existing `formatDuration` and `sessionDurationMs` helpers. Keep the header height compact and responsive, with wrapping metadata on narrow screens. Do not include trace labels, trace controls, zoom controls, or hero copy.
  </action>
  <verify>
    <automated>cd /Users/duytran/GitHub/hooker/frontend && pnpm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Header includes breadcrumb text `Projects`
    - Header includes the current project name or cwd-derived fallback
    - Header includes a shortened current session ID
    - Header includes started time and duration when session metadata is available
    - Header includes `{count} files changed` based on `groups.length`
  </acceptance_criteria>
  <done>Compact header renders required session facts without trace controls</done>
</task>

</tasks>

<verification>
- `cd /Users/duytran/GitHub/hooker/frontend && pnpm run typecheck`
- `cd /Users/duytran/GitHub/hooker/frontend && pnpm run lint`
- `cd /Users/duytran/GitHub/hooker/frontend && pnpm run test -- tests/features/sessions`
- Grep verification: `TraceViewPage.tsx` must not contain `TraceTreeNode`, `EventTimeline`, `TraceInspectionPanel`, `useTraces`, `zoom`, or `react-resizable-panels`.
</verification>

<success_criteria>
- SESS-01: The route no longer renders the trace/timeline page and instead renders the file-change browser shell.
- SESS-03: The page uses the existing file-change API/data first and does not require backend work for old/new fields.
- UI-SPEC: The route uses a compact header and omits trace/timeline/inspection UI.
</success_criteria>
