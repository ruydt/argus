---
phase: 03-mature-local-product
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/hooker
  - docs/privacy.md
  - docs/security.md
  - docs/install.md
  - docs/quickstart.md
  - README.md
autonomous: true
requirements: [SEC-04, PRIV-02, PRIV-03]
must_haves:
  truths:
    - "Doctor output includes a clear privacy warning before capture starts."
    - "Setup and install docs list prompts, diffs, file paths, tool outputs, raw payloads, and exports as sensitive captured data."
    - "Export docs warn that NDJSON and SQLite snapshots contain full-fidelity sensitive data."
    - "Threat model docs state localhost-only, single-user trust model, no auth for loopback use, and unsupported remote/ngrok sharing."
  artifacts:
    - path: "scripts/hooker"
      provides: "Doctor privacy warning"
      contains: "raw payloads"
    - path: "docs/privacy.md"
      provides: "Privacy controls and export implications"
      contains: "~/.config/hooker/ignore"
    - path: "docs/security.md"
      provides: "Local threat model"
      contains: "single-user"
    - path: "docs/install.md"
      provides: "Setup docs privacy warning"
      contains: "exports"
  key_links:
    - from: "docs/privacy.md"
      to: "scripts/hooker"
      via: "same captured-data categories"
      pattern: "prompts.*diffs.*file paths.*tool outputs.*raw payloads.*exports"
    - from: "docs/security.md"
      to: ".planning/REQUIREMENTS.md"
      via: "SEC-04 threat model"
      pattern: "ngrok|remote sharing|unsupported"
---

<objective>
Document privacy controls, export implications, and the local threat model.

Purpose: Users should understand what hooker captures before enabling hooks and should not mistake local-first defaults for remote-safe deployment.
Output: Doctor privacy warning, detailed privacy/security docs, and setup docs that link to the new guidance.
</objective>

<execution_context>
@/Users/duytran/.codex/get-shit-done/workflows/execute-plan.md
@/Users/duytran/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-mature-local-product/03-CONTEXT.md
@.planning/phases/03-mature-local-product/03-RESEARCH.md
@.planning/phases/03-mature-local-product/03-PATTERNS.md
@scripts/hooker
@docs/install.md
@docs/quickstart.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add doctor and setup privacy warnings</name>
  <files>scripts/hooker, docs/install.md, docs/quickstart.md, README.md</files>
  <read_first>
    - `.planning/phases/03-mature-local-product/03-CONTEXT.md` decisions D-09 and specifics privacy category list
    - `.planning/phases/03-mature-local-product/03-PATTERNS.md` sections for `scripts/hooker` and `docs/privacy.md`
    - `scripts/hooker`
    - `docs/install.md`
    - `docs/quickstart.md`
    - `README.md`
  </read_first>
  <action>Extend `./scripts/hooker doctor` report-only output with a privacy warning that names prompts, diffs, file paths, tool outputs, raw payloads, and exports. Keep the existing required-vs-optional doctor style and do not make the warning a failure. Update `docs/install.md` and `docs/quickstart.md` so the setup path tells users what categories are captured before they send the first hook event. Keep README short: add or update only a link to `docs/privacy.md` and `docs/security.md`, not a full privacy manual.</action>
  <verify>
    <automated>bash -n scripts/hooker</automated>
    <automated>./scripts/hooker doctor || true</automated>
  </verify>
  <acceptance_criteria>
    - CLI output assertion: `./scripts/hooker doctor || true` prints `prompts`, `diffs`, `file paths`, `tool outputs`, `raw payloads`, and `exports`.
    - Source assertion: `docs/install.md` includes all six captured/export category strings.
    - Source assertion: `docs/quickstart.md` links to `docs/privacy.md` or uses `privacy.md` in a relative link.
    - Source assertion: `README.md` stays concise and links to privacy/security docs instead of duplicating them.
  </acceptance_criteria>
  <done>Users see explicit capture categories in doctor and setup docs before capture begins.</done>
</task>

<task type="auto">
  <name>Task 2: Create privacy and security posture docs</name>
  <files>docs/privacy.md, docs/security.md, docs/install.md</files>
  <read_first>
    - `.planning/phases/03-mature-local-product/03-CONTEXT.md` decisions D-01 through D-09
    - `.planning/phases/03-mature-local-product/03-RESEARCH.md` Security Domain and Known Threat Patterns
    - `.planning/phases/03-mature-local-product/03-PATTERNS.md` sections for `docs/privacy.md` and `docs/security.md`
    - `docs/install.md`
    - `.planning/REQUIREMENTS.md` SEC-04, PRIV-01, PRIV-03
  </read_first>
  <action>Create `docs/privacy.md` with the default ignore path `~/.config/hooker/ignore`, `$HOOKER_IGNORE`, matching scope limited to `cwd` and explicit `path`, no scanning of prompts/tool output/diffs/raw payload text, ignored events having no DB row and no SSE broadcast, and export warnings for NDJSON and SQLite snapshots. Create `docs/security.md` with the threat model: localhost-use only, single-user trust model, no auth for loopback use, Host header guard, CORS allowlist, loopback bind default, `HOOKER_ALLOW_REMOTE=1` remote opt-in, and remote sharing/ngrok as unofficial and unsupported. Update `docs/install.md` to link both docs from its configuration/privacy sections.</action>
  <verify>
    <automated>test -f docs/privacy.md && test -f docs/security.md</automated>
    <automated>rg -n 'localhost-use|single-user|no auth|unsupported|~/.config/hooker/ignore|HOOKER_IGNORE|NDJSON|snapshot' docs/privacy.md docs/security.md docs/install.md</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `docs/privacy.md` contains `~/.config/hooker/ignore`, `HOOKER_IGNORE`, `cwd`, `path`, `NDJSON`, and `snapshot`.
    - Source assertion: `docs/security.md` contains `localhost`, `single-user`, `no auth`, `HOOKER_ALLOW_REMOTE=1`, `ngrok`, and `unsupported`.
    - Source assertion: `docs/privacy.md` explicitly says prompts, tool output, diffs, and raw payload text are not scanned for path-like substrings.
    - Source assertion: `docs/install.md` links to both `docs/privacy.md` and `docs/security.md`.
  </acceptance_criteria>
  <done>SEC-04, PRIV-02, and PRIV-03 are covered by concrete docs and CLI warning surfaces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/CLI -> user operational behavior | Documentation and doctor output influence whether users expose or export sensitive local data. |
| local data -> export files | NDJSON and SQLite snapshots move sensitive data out of the app database into portable files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Information Disclosure | `scripts/hooker doctor` | mitigate | Doctor warning lists captured/exported sensitive categories before use; warning remains report-only. |
| T-03-04-02 | Information Disclosure | `docs/privacy.md` export section | mitigate | Document that NDJSON and snapshots contain prompts, diffs, file paths, tool outputs, raw payloads, and should be handled as sensitive. |
| T-03-04-03 | Spoofing | `docs/security.md` remote posture | mitigate | Document that remote sharing/ngrok is unsupported and no auth is provided for loopback use. |
| T-03-04-04 | Tampering | documentation integrity | mitigate | Verify exact source strings and links with `rg`; keep README concise and canonical docs under `docs/`. |
</threat_model>

<verification>
Run `bash -n scripts/hooker`, `./scripts/hooker doctor || true`, and `rg -n 'prompts|diffs|file paths|tool outputs|raw payloads|exports|unsupported|HOOKER_ALLOW_REMOTE=1' scripts/hooker docs README.md`.
</verification>

<success_criteria>
Users can find explicit privacy controls, export warnings, and a truthful local threat model without reading source code.
</success_criteria>

<output>
Create `.planning/phases/03-mature-local-product/03-04-SUMMARY.md` when done.
</output>
