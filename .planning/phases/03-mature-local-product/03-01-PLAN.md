---
phase: 03-mature-local-product
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [PRIV-01]
must_haves:
  truths:
    - "The gitignore matcher approach is explicitly approved before any dependency install or in-tree matcher implementation."
    - "PRIV-01 implementation has a deterministic path for gitignore-like blank lines, comments, negation, directory patterns, and **."
  artifacts:
    - path: ".planning/phases/03-mature-local-product/03-01-SUMMARY.md"
      provides: "Recorded matcher decision for downstream privacy implementation"
      contains: "matcher decision"
  key_links:
    - from: ".planning/phases/03-mature-local-product/03-01-SUMMARY.md"
      to: ".planning/phases/03-mature-local-product/03-02-PLAN.md"
      via: "dependency decision consumed by privacy implementation"
      pattern: "github.com/git-pkgs/gitignore|internal matcher"
---

<objective>
Resolve the gitignore matcher dependency gate before implementation.

Purpose: PRIV-01 depends on gitignore-like semantics, and research flagged `github.com/git-pkgs/gitignore@v1.2.0` for human approval because package tooling misclassified Go modules as npm packages.
Output: A recorded decision that either approves the vetted Go dependency or directs Plan 03-02 to implement the scoped internal matcher.
</objective>

<execution_context>
@/Users/duytran/.codex/get-shit-done/workflows/execute-plan.md
@/Users/duytran/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-mature-local-product/03-CONTEXT.md
@.planning/phases/03-mature-local-product/03-RESEARCH.md
@.planning/phases/03-mature-local-product/03-PATTERNS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Approve or reject the gitignore matcher dependency</name>
  <files>.planning/phases/03-mature-local-product/03-01-SUMMARY.md</files>
  <read_first>
    - `.planning/phases/03-mature-local-product/03-CONTEXT.md` decisions D-01 through D-05
    - `.planning/phases/03-mature-local-product/03-RESEARCH.md` Package Legitimacy Audit, Standard Stack, Don't Hand-Roll
    - `.planning/phases/03-mature-local-product/03-PATTERNS.md` privacy ignore package/test patterns
  </read_first>
  <action>Present the user with the Phase 3 matcher choice: approve `github.com/git-pkgs/gitignore@v1.2.0` for `backend/go.mod` because it supports comments, `!` negation, directory patterns, and `**`; or reject it and require Plan 03-02 to build a scoped internal matcher covering only D-05 semantics. State that `slopcheck` reported `[SLOP]` only because it checked npm, not Go module registries, and that `rtk go list -m -json github.com/git-pkgs/gitignore@v1.2.0` verified module metadata. Do not run `rtk go get` in this checkpoint plan.</action>
  <verify>
    <automated>MISSING - checkpoint only; executor must record the selected decision in `.planning/phases/03-mature-local-product/03-01-SUMMARY.md`.</automated>
    <human-check>User selects exactly one option: approve `github.com/git-pkgs/gitignore@v1.2.0` or reject dependency and use scoped internal matcher.</human-check>
  </verify>
  <acceptance_criteria>
    - Source assertion: `.planning/phases/03-mature-local-product/03-01-SUMMARY.md` contains `matcher decision: approved github.com/git-pkgs/gitignore@v1.2.0` or `matcher decision: internal matcher`.
    - Source assertion: summary includes the reason for the decision and references D-05.
    - Behavior assertion: no `backend/go.mod` or `backend/go.sum` changes are made by this checkpoint plan.
  </acceptance_criteria>
  <done>Downstream Plan 03-02 can implement PRIV-01 without asking again which matcher path to use.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human decision -> dependency policy | A package approval decision controls whether execution may install a new Go module. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | matcher dependency approval | mitigate | Blocking human verification before any `rtk go get`; record exact module path and version in the summary. |
| T-03-01-02 | Repudiation | checkpoint result | mitigate | Summary must contain the selected option and rationale so later executors can audit why the dependency path was used. |
| T-03-01-SC | Tampering | Go module install | mitigate | Treat research `[SUS]` package status as requiring human approval; `[SLOP]` npm result is not accepted as Go evidence. |
</threat_model>

<verification>
Confirm `.planning/phases/03-mature-local-product/03-01-SUMMARY.md` records one selected matcher path and that `git diff -- backend/go.mod backend/go.sum` is empty after this plan.
</verification>

<success_criteria>
PRIV-01 implementation has an approved matcher strategy, and no package install happens before the approval checkpoint.
</success_criteria>

<output>
Create `.planning/phases/03-mature-local-product/03-01-SUMMARY.md` when done.
</output>
