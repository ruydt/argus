---
phase: 09-frontend-test-coverage-docs-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/tests/features/diagnostics/DiagnosticsPage.test.tsx
autonomous: true
requirements:
  - TEST-01
user_setup: []
must_haves:
  truths:
    - "D-01: Fill the exact requirement gaps first for DiagnosticsPage loading, error, healthy, and degraded states."
    - "D-02: Treat already-covered DiagnosticsPage branches as done and avoid duplicate test churn."
    - "D-03: Prefer user-visible DiagnosticsPage assertions over internal hook state."
    - "D-04: Include nearby DiagnosticsPage edge states only when already exposed and low-cost."
    - "D-05: Keep the existing DiagnosticsPage suite as the base and audit it before adding tests."
    - "D-06: Preserve Phase 6 operator UI contracts: skeleton first load, compact retry, warning badges, first-run hint, and visible data during refresh."
    - "D-07: Continue using vi.stubGlobal('fetch'), MemoryRouter, Testing Library DOM queries, and navigator.clipboard stubbing where copy affordances render."
    - "DiagnosticsPage Vitest coverage proves loading, error, healthy, and degraded rendering states."
    - "DiagnosticsPage tests preserve Phase 6 operator UI contracts without redesigning production UI."
  artifacts:
    - path: "frontend/tests/features/diagnostics/DiagnosticsPage.test.tsx"
      provides: "DiagnosticsPage rendering-state coverage"
      contains: "DiagnosticsPage"
  key_links:
    - from: "frontend/tests/features/diagnostics/DiagnosticsPage.test.tsx"
      to: "frontend/src/features/diagnostics/DiagnosticsPage.tsx"
      via: "Testing Library render under MemoryRouter"
      pattern: "render\\(\\s*<MemoryRouter>\\s*<DiagnosticsPage"
    - from: "frontend/tests/features/diagnostics/DiagnosticsPage.test.tsx"
      to: "/api/diagnostics"
      via: "vi.stubGlobal('fetch')"
      pattern: "vi\\.stubGlobal\\('fetch'"
---

<objective>
Audit and tighten DiagnosticsPage Vitest coverage for TEST-01 without duplicating existing state tests.

Purpose: TEST-01 is already mostly covered; this plan proves the named branches with user-visible assertions and adds only missing assertions needed to make the coverage unambiguous.
Output: Updated DiagnosticsPage test suite or a summary proving no test edits were needed after audit.
</objective>

<execution_context>
@/Users/duytran/.codex/get-shit-done/workflows/execute-plan.md
@/Users/duytran/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-frontend-test-coverage-docs-cleanup/09-CONTEXT.md
@.planning/phases/09-frontend-test-coverage-docs-cleanup/09-RESEARCH.md
@.planning/phases/09-frontend-test-coverage-docs-cleanup/09-PATTERNS.md
@.planning/phases/09-frontend-test-coverage-docs-cleanup/09-UI-SPEC.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md
@frontend/src/features/diagnostics/DiagnosticsPage.tsx
@frontend/src/features/diagnostics/hooks/useDiagnostics.ts
@frontend/src/features/diagnostics/types.ts
@frontend/tests/features/diagnostics/DiagnosticsPage.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit DiagnosticsPage branch coverage</name>
  <files>frontend/tests/features/diagnostics/DiagnosticsPage.test.tsx</files>
  <read_first>
    <file>frontend/tests/features/diagnostics/DiagnosticsPage.test.tsx</file>
    <file>frontend/src/features/diagnostics/DiagnosticsPage.tsx</file>
    <file>frontend/src/features/diagnostics/hooks/useDiagnostics.ts</file>
    <file>frontend/src/features/diagnostics/types.ts</file>
    <file>.planning/phases/09-frontend-test-coverage-docs-cleanup/09-CONTEXT.md</file>
    <file>.planning/phases/09-frontend-test-coverage-docs-cleanup/09-PATTERNS.md</file>
    <file>.planning/phases/09-frontend-test-coverage-docs-cleanup/09-UI-SPEC.md</file>
  </read_first>
  <action>Map the existing `DiagnosticsPage.test.tsx` cases to TEST-01 and decisions D-01 through D-07. Confirm coverage for loading, error, healthy, and degraded states using user-visible assertions: `Diagnostics`, `Failed to load diagnostics`, `Could not reach /api/diagnostics`, `Retry Load`, `Agent Connectivity`, `System Facts`, `Degraded`, and extra CORS origin text. Treat existing adequate tests as complete per D-02 and do not add duplicate cases.</action>
  <acceptance_criteria>
    <criterion>Source assertion: `frontend/tests/features/diagnostics/DiagnosticsPage.test.tsx` contains tests that render `DiagnosticsPage` under `MemoryRouter` and use `vi.stubGlobal('fetch')`.</criterion>
    <criterion>Source assertion: TEST-01 branch mapping is represented by assertions for loading, error, healthy, and degraded states in the test file or documented in the plan summary as already covered.</criterion>
    <criterion>Behavior assertion: no production file under `frontend/src/features/diagnostics/` is changed unless a required state test exposes a real bug or testability blocker.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>rtk bash -lc 'cd frontend && pnpm test --run tests/features/diagnostics/DiagnosticsPage.test.tsx'</automated>
  </verify>
  <done>DiagnosticsPage TEST-01 branch coverage is proven with existing or tightened user-visible assertions, and the targeted suite passes.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten missing DiagnosticsPage assertions only if audit finds a gap</name>
  <files>frontend/tests/features/diagnostics/DiagnosticsPage.test.tsx</files>
  <read_first>
    <file>frontend/tests/features/diagnostics/DiagnosticsPage.test.tsx</file>
    <file>frontend/src/features/diagnostics/DiagnosticsPage.tsx</file>
    <file>.planning/phases/09-frontend-test-coverage-docs-cleanup/09-CONTEXT.md</file>
    <file>.planning/phases/09-frontend-test-coverage-docs-cleanup/09-UI-SPEC.md</file>
  </read_first>
  <action>If Task 1 finds shallow coverage, add the smallest user-visible assertions needed. Loading must keep heading `Diagnostics` visible and must keep `Agent Connectivity` and `System Facts` absent until data loads; if the DOM exposes `aria-busy="true"`, assert it. Error must assert `Failed to load diagnostics`, `Could not reach /api/diagnostics`, and a `Retry Load` button. Healthy must assert `Agent Connectivity`, `System Facts`, readiness/configured agent content, and export warning content. Degraded must assert `Degraded`, extra CORS origin text, and visible warning/security posture badges. Preserve `navigator.clipboard` stubbing and `vi.stubGlobal('fetch')` style per D-07.</action>
  <acceptance_criteria>
    <criterion>Source assertion: no new DiagnosticsPage test repeats an existing test without adding at least one TEST-01-relevant assertion.</criterion>
    <criterion>Source assertion: any added assertions use Testing Library DOM queries such as `screen.getByRole`, `screen.findByText`, `screen.queryByText`, or `waitFor`; they do not assert private hook variables.</criterion>
    <criterion>CLI assertion: targeted DiagnosticsPage Vitest command exits with status 0.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>rtk bash -lc 'cd frontend && pnpm test --run tests/features/diagnostics/DiagnosticsPage.test.tsx'</automated>
  </verify>
  <done>Any DiagnosticsPage gaps found by audit are closed without production UI redesign, duplicate churn, or internal-state assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fetch stub -> DiagnosticsPage | Mocked `/api/diagnostics` data crosses into UI rendering code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Tampering | `frontend/tests/features/diagnostics/DiagnosticsPage.test.tsx` | mitigate | Use structured `Diagnostics` fixtures and user-visible assertions so tests cannot pass by over-mocking the component under test. |
| T-09-01-02 | Information Disclosure | Diagnostics fixtures | accept | Fixtures contain synthetic paths and no real prompts, secrets, or payload content; no high-severity threat introduced. |
| T-09-01-SC | Tampering | npm installs | accept | No package install is planned in this test-only plan. |
</threat_model>

<verification>
Run `rtk bash -lc 'cd frontend && pnpm test --run tests/features/diagnostics/DiagnosticsPage.test.tsx'`.
</verification>

<success_criteria>
TEST-01 is traceably satisfied: DiagnosticsPage loading, error, healthy, and degraded branches are covered by passing Vitest assertions.
</success_criteria>

<output>
Create `.planning/phases/09-frontend-test-coverage-docs-cleanup/09-01-SUMMARY.md` when done.
</output>
