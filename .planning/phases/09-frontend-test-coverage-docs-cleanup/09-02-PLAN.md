---
phase: 09-frontend-test-coverage-docs-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/tests/features/usage/UsagePage.test.tsx
autonomous: true
requirements:
  - TEST-02
user_setup: []
must_haves:
  truths:
    - "D-01: Fill the exact requirement gaps first for UsagePage loading, empty, and populated states."
    - "D-03: Prefer user-visible UsagePage assertions over internal hook state."
    - "D-08: Exercise the real UsagePage, UsagePanel, and useOpenAIUsage path rather than mocking the hook."
    - "D-09: Use localStorage and fetch stubs with small realistic fixtures for UsageCharts and UsageTables."
    - "D-10: Empty state means no admin API key and the existing Admin API Key Required panel is rendered."
    - "D-11: Loading state uses a present key plus pending fetch and asserts visible loading UI, not private variables."
    - "D-12: Populated state proves successful usage responses render visible chart/table summary content without exhaustive provider pagination coverage."
    - "UsagePage Vitest coverage proves loading, empty, and populated rendering states."
    - "UsagePage tests exercise the real UsagePage, UsagePanel, and useOpenAIUsage path."
  artifacts:
    - path: "frontend/tests/features/usage/UsagePage.test.tsx"
      provides: "UsagePage loading, empty, and populated state coverage"
      contains: "UsagePage"
  key_links:
    - from: "frontend/tests/features/usage/UsagePage.test.tsx"
      to: "frontend/src/features/usage/UsagePage.tsx"
      via: "render under MemoryRouter"
      pattern: "<UsagePage"
    - from: "frontend/tests/features/usage/UsagePage.test.tsx"
      to: "frontend/src/features/usage/hooks/useOpenAIUsage.ts"
      via: "real hook with localStorage and fetch stubs"
      pattern: "openai_admin_key"
---

<objective>
Extend UsagePage Vitest coverage for TEST-02 through the real page, panel, hook, chart, and table path.

Purpose: Existing UsagePage tests prove the empty shell only; TEST-02 requires loading and populated behavior as well.
Output: Updated `frontend/tests/features/usage/UsagePage.test.tsx` with loading and populated state tests.
</objective>

<execution_context>
@/Users/duytran/.codex/get-shit-done/workflows/execute-plan.md
@/Users/duytran/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-frontend-test-coverage-docs-cleanup/09-CONTEXT.md
@.planning/phases/09-frontend-test-coverage-docs-cleanup/09-RESEARCH.md
@.planning/phases/09-frontend-test-coverage-docs-cleanup/09-PATTERNS.md
@.planning/phases/09-frontend-test-coverage-docs-cleanup/09-UI-SPEC.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md
@frontend/src/features/usage/UsagePage.tsx
@frontend/src/features/usage/UsagePanel.tsx
@frontend/src/features/usage/hooks/useOpenAIUsage.ts
@frontend/src/features/usage/UsageCharts.tsx
@frontend/src/features/usage/UsageTables.tsx
@frontend/src/types/usage.ts
@frontend/tests/features/usage/UsagePage.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add UsagePage loading state coverage</name>
  <files>frontend/tests/features/usage/UsagePage.test.tsx</files>
  <read_first>
    <file>frontend/tests/features/usage/UsagePage.test.tsx</file>
    <file>frontend/src/features/usage/UsagePage.tsx</file>
    <file>frontend/src/features/usage/UsagePanel.tsx</file>
    <file>frontend/src/features/usage/hooks/useOpenAIUsage.ts</file>
    <file>.planning/phases/09-frontend-test-coverage-docs-cleanup/09-CONTEXT.md</file>
    <file>.planning/phases/09-frontend-test-coverage-docs-cleanup/09-PATTERNS.md</file>
    <file>.planning/phases/09-frontend-test-coverage-docs-cleanup/09-UI-SPEC.md</file>
  </read_first>
  <action>Extend the existing `UsagePage.test.tsx` setup instead of mocking `useOpenAIUsage`, per D-08. For loading, configure `localStorageMock.getItem` to return `sk-test` only for `openai_admin_key`, keep `anthropic_admin_key` and cache keys empty, and stub `fetch` with a never-resolving promise. Render `UsagePage` under `MemoryRouter` and assert the page heading `OpenAI Usage`, the disabled button named `Loading...`, and the panel text `Loading usage data...`. Preserve `vi.stubGlobal('localStorage')`, `vi.stubGlobal('fetch')`, and non-concurrent Vitest tests.</action>
  <acceptance_criteria>
    <criterion>Source assertion: `UsagePage.test.tsx` has a test for loading that uses a pending fetch promise and does not use `vi.mock` for `@/features/usage/hooks/useOpenAIUsage`.</criterion>
    <criterion>Behavior assertion: loading test asserts `screen.getByRole('button', { name: 'Loading...' })` is disabled or equivalent Testing Library role/name assertion.</criterion>
    <criterion>Behavior assertion: loading test asserts `Loading usage data...` is present while the fetch remains pending.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>rtk bash -lc 'cd frontend && pnpm test --run tests/features/usage/UsagePage.test.tsx'</automated>
  </verify>
  <done>UsagePage loading branch is covered through real localStorage and fetch state.</done>
</task>

<task type="auto">
  <name>Task 2: Add UsagePage populated state coverage</name>
  <files>frontend/tests/features/usage/UsagePage.test.tsx</files>
  <read_first>
    <file>frontend/tests/features/usage/UsagePage.test.tsx</file>
    <file>frontend/src/features/usage/UsagePanel.tsx</file>
    <file>frontend/src/features/usage/hooks/useOpenAIUsage.ts</file>
    <file>frontend/src/features/usage/UsageCharts.tsx</file>
    <file>frontend/src/features/usage/UsageTables.tsx</file>
    <file>frontend/src/types/usage.ts</file>
    <file>.planning/phases/09-frontend-test-coverage-docs-cleanup/09-CONTEXT.md</file>
    <file>.planning/phases/09-frontend-test-coverage-docs-cleanup/09-RESEARCH.md</file>
    <file>.planning/phases/09-frontend-test-coverage-docs-cleanup/09-PATTERNS.md</file>
  </read_first>
  <action>Add a populated OpenAI usage test that drives the real hook with three successful fetch responses: primary completions usage, model-grouped usage, and api-key-grouped usage. Use a deterministic day such as Unix seconds for `2026-05-31T00:00:00Z`. Include non-empty `data` arrays with at least one `results` entry so aggregation produces visible totals, a model name `gpt-test`, and an API key ID `key-test`. Assert `Total Tokens`, `Total Requests`, `Model Breakdown`, `API Key Breakdown`, `gpt-test`, and `key-test`. Keep the existing empty-state test asserting `Admin API Key Required` and local-storage disclosure copy per D-10.</action>
  <acceptance_criteria>
    <criterion>Source assertion: populated test configures three ordered successful `fetch` responses before rendering `UsagePage`.</criterion>
    <criterion>Behavior assertion: populated test proves charts and tables render by asserting `Total Tokens`, `Total Requests`, `Model Breakdown`, and `API Key Breakdown`.</criterion>
    <criterion>Behavior assertion: populated test asserts fixture values `gpt-test` and `key-test` are visible.</criterion>
    <criterion>CLI assertion: targeted UsagePage Vitest command exits with status 0.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>rtk bash -lc 'cd frontend && pnpm test --run tests/features/usage/UsagePage.test.tsx'</automated>
  </verify>
  <done>UsagePage empty, loading, and populated branches satisfy TEST-02 through real page/hook behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test localStorage/fetch stubs -> UsagePage | Fake admin key and fake usage responses drive UI behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Information Disclosure | `UsagePage.test.tsx` fixtures | mitigate | Use fake key string `sk-test`; do not read environment variables or real credentials. |
| T-09-02-02 | Tampering | UsagePage coverage | mitigate | Exercise real `UsagePage` / `UsagePanel` / `useOpenAIUsage` path and do not mock the hook, per D-08. |
| T-09-02-SC | Tampering | npm installs | accept | No package install is planned in this test-only plan. |
</threat_model>

<verification>
Run `rtk bash -lc 'cd frontend && pnpm test --run tests/features/usage/UsagePage.test.tsx'`.
</verification>

<success_criteria>
TEST-02 is traceably satisfied: UsagePage loading, empty, and populated branches are covered by passing Vitest assertions.
</success_criteria>

<output>
Create `.planning/phases/09-frontend-test-coverage-docs-cleanup/09-02-SUMMARY.md` when done.
</output>
