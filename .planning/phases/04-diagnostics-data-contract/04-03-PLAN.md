---
phase: 04-diagnostics-data-contract
plan: 03
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - backend/tests/internal/repository/sqlite/sqlite_test.go
  - backend/tests/internal/service/event_service_test.go
  - backend/tests/internal/handler/diagnostics_test.go
  - backend/tests/internal/server/router_test.go
autonomous: true
requirements:
  - DIAG-01
  - DIAG-02
  - DIAG-03
  - DIAG-04
  - TEST-01
user_setup: []
must_haves:
  truths:
    - TEST-01: backend tests cover diagnostics response shape, DB stats, readiness state, empty DB behavior, and aggregate query behavior.
    - DIAG-01/DIAG-02/DIAG-03: tests prove `GET /api/diagnostics` returns version metadata, health/readiness, DB path/size, total event count, total session count, and latest event timestamp.
    - DIAG-04/D-18: tests prove diagnostics aggregation uses repository aggregate behavior rather than list-loading service paths.
    - D-02/D-14: tests prove Phase 4 response has no `agents`, `privacy`, raw payload, prompt, diff, command, tool output, or arbitrary captured text fields.
  artifacts:
    - backend/tests/internal/repository/sqlite/sqlite_test.go
    - backend/tests/internal/service/event_service_test.go
    - backend/tests/internal/handler/diagnostics_test.go
    - backend/tests/internal/server/router_test.go
  key_links:
    - Handler and router tests exercise the endpoint shape created in Plan 02.
    - Repository tests exercise the aggregate method created in Plan 01.
---

<objective>
Add backend regression coverage for the diagnostics data contract.

Purpose: prove the Phase 4 endpoint and repository behavior satisfy the contract before UI and Phase 5 build on it.
Output: repository, service, handler, and router tests covering response shape, storage stats, empty DB, readiness, privacy boundary, and aggregate behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-diagnostics-data-contract/04-CONTEXT.md
@.planning/phases/04-diagnostics-data-contract/04-RESEARCH.md
@.planning/phases/04-diagnostics-data-contract/04-PATTERNS.md
@.planning/phases/04-diagnostics-data-contract/04-01-SUMMARY.md
@.planning/phases/04-diagnostics-data-contract/04-02-SUMMARY.md
@backend/tests/internal/repository/sqlite/sqlite_test.go
@backend/tests/internal/service/event_service_test.go
@backend/tests/internal/handler/hook_test.go
@backend/tests/internal/handler/export_test.go
@backend/tests/internal/server/router_test.go
</context>

<threat_model>
Threat: tests could verify happy-path shape but miss sensitive captured-content leakage.
Severity: high.
Mitigation: add explicit negative assertions against sensitive key names and seeded sensitive values in diagnostics handler response tests.

Threat: tests could pass even if diagnostics uses list-loading paths internally.
Severity: medium.
Mitigation: add service/mock tests where diagnostics succeeds via `DiagnosticsStorageStats` and would fail if it called list/dashboard methods.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Cover SQLite diagnostics aggregate behavior</name>
  <files>backend/tests/internal/repository/sqlite/sqlite_test.go</files>
  <read_first>backend/tests/internal/repository/sqlite/sqlite_test.go, backend/internal/repository/sqlite/sqlite.go, backend/internal/domain/diagnostics.go</read_first>
  <action>Add repository tests for `DiagnosticsStorageStats`. Cover empty DB returning `TotalEvents == 0`, `TotalSessions == 0`, and `LatestEventAt == nil`. Add stored events with deliberately out-of-order timestamps and assert `LatestEventAt` equals the maximum `created_at` timestamp, not insertion order. Add at least one stored degraded event with `NormalizationStatus: "degraded"` and assert it counts in `TotalEvents`. Use `UpsertSession` to create sessions and assert `TotalSessions` counts rows in `sessions`.</action>
  <verify>(workdir: backend) rtk go test ./tests/internal/repository/sqlite -run 'Test.*Diagnostics'</verify>
  <acceptance_criteria>
    - `backend/tests/internal/repository/sqlite/sqlite_test.go` contains `DiagnosticsStorageStats`.
    - A test asserts empty DB `LatestEventAt == nil`.
    - A test asserts a degraded stored event contributes to `TotalEvents`.
    - A test asserts latest event uses the later timestamp when events are inserted out of order.
    - A test asserts `TotalSessions` changes after `UpsertSession`.
  </acceptance_criteria>
  <done>SQLite aggregate behavior is covered independently of handler/service wiring.</done>
</task>

<task type="auto">
  <name>Task 2: Cover service diagnostics composition</name>
  <files>backend/tests/internal/service/event_service_test.go</files>
  <read_first>backend/tests/internal/service/event_service_test.go, backend/internal/service/event_service.go, backend/internal/domain/diagnostics.go</read_first>
  <action>Extend `mockRepo` with fields for diagnostics storage stats, diagnostics error, and call count if not already present from Plan 01. Add service tests proving `Diagnostics(":memory:", false)` returns `Health.Live == true`, `Health.Ready == false`, `Health.Reason == "database not ready"`, `Storage.DBPath == ":memory:"`, `Storage.DBSizeBytes == nil`, and `Storage.DBSizeReason == "unavailable"`. Add a temp-file DB path case or service-level file path case proving measurable `DBSizeBytes` is non-nil when `os.Stat` succeeds. Add assertions that service copies `TotalEvents`, `TotalSessions`, and `LatestEventAt` from the repo stats.</action>
  <verify>(workdir: backend) rtk go test ./tests/internal/service -run 'Test.*Diagnostics'</verify>
  <acceptance_criteria>
    - `backend/tests/internal/service/event_service_test.go` contains a test for not-ready diagnostics reason `database not ready`.
    - `backend/tests/internal/service/event_service_test.go` contains a test for `DBSizeBytes == nil` with `:memory:`.
    - `backend/tests/internal/service/event_service_test.go` contains a test for non-nil `DBSizeBytes` with a real temp file path.
    - `backend/tests/internal/service/event_service_test.go` asserts repo diagnostics stats are copied into the service response.
  </acceptance_criteria>
  <done>Service composition handles readiness, DB size nullability, and aggregate propagation.</done>
</task>

<task type="auto">
  <name>Task 3: Cover diagnostics handler and router response shape</name>
  <files>backend/tests/internal/handler/diagnostics_test.go, backend/tests/internal/server/router_test.go</files>
  <read_first>backend/tests/internal/handler/hook_test.go, backend/tests/internal/handler/export_test.go, backend/tests/internal/server/router_test.go, backend/internal/handler/diagnostics.go, backend/internal/server/router.go</read_first>
  <action>Create `backend/tests/internal/handler/diagnostics_test.go` if it does not exist. Add handler tests using `httptest` that decode JSON and assert top-level `version`, `health`, and `storage` groups exist. Assert no top-level `agents` or `privacy` keys exist. Seed or simulate sensitive strings such as `secret prompt`, `rm -rf`, `raw_payload`, `tool_result_stdout`, and assert the diagnostics response body does not contain those strings or sensitive key names. Add a not-ready handler test asserting HTTP 200 and `health.ready == false`. Extend `backend/tests/internal/server/router_test.go` with a `GET /api/diagnostics` route smoke test through `newTestRouter`; set or tolerate test `DBPath` so the response decodes successfully.</action>
  <verify>(workdir: backend) rtk go test ./tests/internal/handler ./tests/internal/server -run 'Test.*Diagnostics|TestNewRouter.*Diagnostics'</verify>
  <acceptance_criteria>
    - `backend/tests/internal/handler/diagnostics_test.go` exists.
    - Handler tests assert `version`, `health`, and `storage` JSON groups.
    - Handler tests assert `agents` and `privacy` are absent.
    - Handler tests assert sensitive captured-content keys or seeded values are absent from the response body.
    - Router tests include `GET /api/diagnostics` and expect HTTP 200.
  </acceptance_criteria>
  <done>HTTP-level diagnostics behavior and privacy boundary are covered.</done>
</task>

<task type="auto">
  <name>Task 4: Run full backend regression for Phase 4</name>
  <files>backend/tests/internal/repository/sqlite/sqlite_test.go, backend/tests/internal/service/event_service_test.go, backend/tests/internal/handler/diagnostics_test.go, backend/tests/internal/server/router_test.go</files>
  <read_first>backend/go.mod, backend/internal/domain/diagnostics.go, backend/internal/repository/repository.go, backend/internal/repository/sqlite/sqlite.go, backend/internal/service/event_service.go, backend/internal/handler/diagnostics.go, backend/internal/server/router.go</read_first>
  <action>Run the full backend test suite with working directory `backend` using `rtk go test ./...`. Fix only failures caused by Phase 4 changes. Do not modify unrelated frontend files or unrelated session trace changes already present in the worktree.</action>
  <verify>(workdir: backend) rtk go test ./...</verify>
  <acceptance_criteria>
    - Full backend Go tests pass.
    - No Phase 4 diagnostics test fails.
    - The existing unrelated modified frontend files remain untouched.
  </acceptance_criteria>
  <done>Phase 4 backend diagnostics contract has regression coverage and passes the backend suite.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `(workdir: backend) rtk go test ./tests/internal/repository/sqlite -run 'Test.*Diagnostics'`
- [ ] `(workdir: backend) rtk go test ./tests/internal/service -run 'Test.*Diagnostics'`
- [ ] `(workdir: backend) rtk go test ./tests/internal/handler ./tests/internal/server -run 'Test.*Diagnostics|TestNewRouter.*Diagnostics'`
- [ ] `(workdir: backend) rtk go test ./...`
- [ ] `rtk rg -n "agents|privacy|raw_payload|prompt|tool_result_stdout|tool_result_stderr" backend/tests/internal/handler/diagnostics_test.go` confirms these are negative assertions, not response fields.
</verification>

<success_criteria>
- Repository aggregate tests cover empty DB, stored rows, degraded rows, and max timestamp.
- Service tests cover readiness false, generic reason, DB size nullability, and aggregate propagation.
- Handler tests cover grouped response shape and captured-content privacy boundary.
- Router test proves `GET /api/diagnostics` is mounted.
- Full backend regression passes.
</success_criteria>

<output>
After completion, create `.planning/phases/04-diagnostics-data-contract/04-03-SUMMARY.md`.
</output>
